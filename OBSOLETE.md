# OBSOLETE — carousel engine v1

Frozen on 2026-09-11. Do not build on this branch.

This is the @suresilly carousel engine as it stood when the account changed
genre: the gated writer, bibliography, safety screens, readability gates,
critic loop, novelty fingerprints, and the Telegram review machinery.

`main` now runs a much smaller easy-going quote-page pipeline. The reasons
and the plan are in `docs/pivot/quote-pages-teardown.md` on `main`.

Kept here for reference only. Nothing on this branch runs: GitHub schedules
fire only on the default branch, and the Cloudflare Worker dispatches `main`.
