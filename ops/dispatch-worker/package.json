{
  "name": "suresilly-dispatch",
  "private": true,
  "version": "1.0.0",
  "description": "Off-GitHub Cron Trigger that fires the @suresilly auto-post workflow via workflow_dispatch.",
  "type": "module",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev --test-scheduled",
    "tail": "wrangler tail",
    "test": "node test/parse-reply.test.mjs && node test/posting-slot.test.mjs"
  },
  "devDependencies": {
    "wrangler": "^4"
  }
}
