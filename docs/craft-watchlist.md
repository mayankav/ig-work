# Craft watchlist

Patterns named in a Telegram `disapprove` or `redo all`. One line each: date, slug, the offending line, the pattern. Twice here means it goes into the editor strike list in `suresilly/craft.md`; three times means `BANNED` or a writer rule. See `.agents/skills/suresilly-writer/SKILL.md`.

